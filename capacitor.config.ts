import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meditate.martincreedy.co.uk',
  appName: 'Simply Meditate',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
